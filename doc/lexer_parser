############################################################
#													       #
#													       #
#				42glands							       #
#													       #
#				"Lexer-parser"							   #
#														   #
############################################################

Réalistation du parser du 42sh:

I/ 	UTILISATION

	Gerer les tokens speciaux d'une ligne de commandes:

II/ IMPLEMENTATION

Recuperer un char* et le transformer intelligemment en **tab/ qqchose ;

III/ COMMENT LE FAIRE

step 1 : recuperer la liste de Tokens
		- creer une liste en ayant la position du token

analyser chaque token et en appliquer les effets sur la commande:

NB : pos -> position du token

	";" -> renvoyer la commande jusqu'a pos a l'execution !
	
	">" -> tout ce qui aller a la sortie standard va dans le fichier decrit;
	
	"0>" -> redirige l'entree standard

	"1>" -> redirige STDOUT

	"2>" - > redirige STDERR

	">>" -> idem mais a la fin du fichier
	
	"<"  -> redirection d'entrée: prendre en entree ce qu'il ya. a droite;
	
	"|" -> utiliser la sortie standard de gauche en tant qu'entree a droite;

	">&" -> redirige vers un autre 

	"\" -> escape le prochain caractere

	""" -> transforme la suite en chaine de caracteres interpretable

	"'" -> transforme la suite en chaine de caracteres non interpretable






